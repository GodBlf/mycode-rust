# MyCode

MyCode is a Rust rewrite of the MewCode Go coding agent. The Headless Core is implemented before the TUI.

## Development

```sh
./scripts/check.sh
```

The check script runs formatting, clippy with warnings denied, and all workspace tests.
